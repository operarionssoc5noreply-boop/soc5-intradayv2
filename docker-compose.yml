services:
  seatalk-bot:
    build: .
    env_file:
      - .env
    ports:
      - "${PORT:-8080}:8080"
    volumes:
      - ./secrets:/secrets:ro
      - seatalk-bot-work:/tmp/seatalk-bot
    restart: unless-stopped

volumes:
  seatalk-bot-work:

